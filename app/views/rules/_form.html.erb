<%= form_for @new_rule, :url => { :action => "create" }, :html => {:class => "well"} do |f| %>
  <%= f.text_area :rule_text, :rows => 2 %>
  <br />
  <%= f.submit "Create", :class => "btn" %>
<% end %>