<%= form_for @new_rule, :url => { :action => "create" }, :html => { :class => "form-horizontal" } do |f| %>
  <%= f.text_field :rule_text, :class => "input-large" %>
  <%= f.button "Create", :class => "btn btn-primary" %>
<% end %>