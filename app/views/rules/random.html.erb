<div class="hero-unit">
  <% if @rule %>
    <h1 id="rule"><%= @rule.rule_text %></h1>
  <% else %>
    <h1>No rules yet :(</h1>
  <% end %>
  <br />
  <p>
    <%= link_to "Give me another!", :root, :remote => true, :class => "btn btn-primary btn-large" %>
  </p>
</div>